import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  input: {
    marginBottom: 14,
    marginTop: 2,
    alignSelf: "center",
  },
  title: {
    marginStart: 24,
    fontWeight: "500",
  },
});

export default styles;
